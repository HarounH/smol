# AGENTS.md

## Environment

Use the project Conda environment before running commands:

```bash
conda activate smol
```

In non-interactive shells where `conda activate` is unavailable, use:

```bash
conda run -n smol <command>
```

## Formatting

Use Black for Python and notebook formatting.

```bash
black .
```
